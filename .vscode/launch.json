{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "lldb",
        "request": "launch",
        "name": "Debug",
        "program": "${workspaceFolder}/run",
        "args": [],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "Build with Clang"
      }
    ]
  }